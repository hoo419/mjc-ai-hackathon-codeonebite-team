# mpu.mjc.ac.kr 연동 — 적성검사 결과 AI 분석 기능 (개발자 A 인계)

## 배경

`/counseling` 화면은 이미 Mock/DB 기반 `careerSummary`/`personalitySummary` 요약과
지도교수/상담사/학과사무실 상담 요청 기능으로 동작한다. 여기서 한 단계 더 나아가, 학생이
실제로 명지전문대 학생역량 이력관리 시스템("SMART CARE", `https://mpu.mjc.ac.kr/`)에서 본
진로적성검사·핵심역량검사·종합심리검사 결과를 우리 AI가 분석해서 보여주는 기능을 추가하고
싶다.

**이번 범위에서 빼는 것**: 학생을 실제 대학 상담사와 실시간으로 연결하는 기능은 상담사 쪽
시스템(예약을 받아서 응답하는 사람/시스템)이 따로 있어야 가능한데 우리 쪽에서 만들 수 있는
영역이 아니다. 지금은 "요청이 접수됐다"를 시뮬레이션하는 기존 `POST /counseling/request`
수준으로 남겨두고, 실제 연결은 **기술적으로 실현 가능하다는 것만 확인**해두는 선에서
정리한다 (아래 "실현 가능성" 절 참고).

## 제약사항 — 왜 "붙여넣기" 방식이어야 하는가

- `mpu.mjc.ac.kr`은 **대학 계정 로그인이 필수**다.
- 이 프로젝트에 붙어있는 Claude(AI 에이전트)는 정책상 어떤 경우에도 아이디/비밀번호를
  입력하거나 로그인을 대행할 수 없고, 백엔드가 학생의 학교 계정 정보를 저장해서 대신
  로그인하는 것도 금지된 방식이라고 봐야 한다(개인정보/보안 문제).
- 즉 서버가 야간에 자동으로 mpu에 접속해서 데이터를 긁어오는 배치 작업은 애초에 설계
  대상이 아니다. **학생이 그 순간 mpu에 직접 로그인해서 검사 결과를 확인하고, 그 텍스트를
  우리 앱에 복사해서 붙여넣는 흐름**으로 설계해야 한다.
- 참고로 이 흐름이 실제로 되는지, Claude Code의 `claude-in-chrome` 확장(사용자가 로그인한
  실제 브라우저 세션을 읽는 도구)으로 직접 로그인 후 화면까지 확인해봤다 — 기술적으로는
  "로그인된 화면의 내용을 읽어서 요약"하는 것 자체는 가능하다는 것도 확인됨(아래 "실현
  가능성" 절). 다만 이건 매번 사람이 직접 로그인해야 하는 라이브 세션에서만 되는 것이라,
  백엔드에 상시 자동화 기능으로 넣을 수 있는 게 아니다.

## 실제 mpu.mjc.ac.kr 구조 조사 결과

로그인 후 직접 확인한 내용 (개인정보 보호를 위해 스크린샷 대신 구조만 텍스트로 기록):

### 마이페이지 — "마이 역량 로드맵" 위젯
학생 정보(이름/학년/지도교수/GPA) 아래에 역량 관련 카드들이 나열되는데, 그중 검사 관련
항목:
- **진로적성검사** — 실시 여부 상태값 표시 (미실시 시 "실시필요" 등)
- **핵심역량검사** — 숫자 점수 표시
- **종합심리검사** — 짧은 텍스트 요약이 카드에 일부 노출됨 (개인 결과라 예시 문구는 생략)

즉 mpu는 검사별로 "상태/점수/짧은 요약"을 따로 보여주지, 우리 계약의
`careerSummary`/`personalitySummary` 같은 통합된 두 문단 요약 형태가 아니다. 학생이
붙여넣을 원문은 이런 개별 카드들의 텍스트를 모은 것이 될 가능성이 높다.

### 상담예약 메뉴 — 4개 카테고리
상단 네비게이션의 "상담예약" 드롭다운에 다음 4개가 있다:
1. 기초조사지
2. 교수님 상담
3. 취업(진로) 상담(잡카페)
4. 심리상담(상담센터)

**우리 `API_CONTRACT.md`의 `CounselingTargetType`(`ADVISOR` / `CAREER_COUNSELOR` /
`DEPARTMENT_OFFICE`)과 완전히 일치하지 않는다** — mpu에는 "심리상담"이 별도 카테고리로
있고 "학과사무실"에 해당하는 항목은 없다. 이 enum을 mpu 체계에 맞출지는 A/B 합의가 필요한
`API_CONTRACT.md` 변경 사항이라 이 문서에서 임의로 결정하지 않았다.

## 개발자 A에게 요청하는 작업

### 1. 신규 엔드포인트 제안: `POST /counseling/analyze-aptitude`

```json
// Request
{
  "rawText": "학생이 mpu.mjc.ac.kr에서 복사해온 검사 결과 원문 (형식 자유)"
}
```

```json
// Response (제안 — 최종 필드명은 개발자 A가 확정)
{
  "summary": "AI가 정리한 한 문단 요약",
  "insights": [
    "핵심 인사이트 1",
    "핵심 인사이트 2"
  ]
}
```

**구현 방향**: 이미 Chat API(`POST /chat`)에서 쓰고 있는 `AI_API_BASE_URL` /
`AI_API_KEY` / `AI_MODEL` (OpenAI-compatible Gateway, `backend/.env`)을 그대로 재사용하면
된다 — 새로운 AI 연동을 처음부터 만들 필요 없이, `rawText`를 프롬프트에 넣고 요약/인사이트를
생성하는 호출 하나만 추가하면 된다. 프롬프트 설계 시 `PROJECT_REQUIREMENTS.md`의 원칙(AI는
사실 데이터를 추측하지 않는다)에 맞춰, "이 텍스트에 없는 내용은 만들어내지 말 것"을
명시하는 걸 권장한다.

**계약 변경 절차**: `API_CONTRACT.md` 13장 규칙대로, 이 엔드포인트를 실제로 추가하기 전에
`API_CONTRACT.md` 문서를 먼저 수정해서 A/B가 필드명·구조에 합의한 뒤 양쪽 구현을 맞출 것.

### 2. `CounselingTargetType` mpu 체계 정합성 (선택 — 우선순위 낮음)

위에서 확인한 mpu의 4개 카테고리와 우리 3개 enum이 다르다는 점만 인지하고 있으면 된다.
지금 당장 맞출 필요는 없고, 시간이 되면 A/B가 상의해서 `API_CONTRACT.md`를 갱신할지
결정한다.

## 실현 가능성 (참고용 — 지금 범위 아님)

`claude-in-chrome` 확장으로 실제 로그인 세션을 읽어서 AI가 요약하는 것 자체는 기술적으로
가능함을 이번에 확인했다. 다만:
- 학생 본인이 그 자리에서 직접 로그인해야만 동작 (자동화/백엔드 상시 기능 불가)
- 실제 상담사와의 연결(사람이 응답하는 것)은 학교 쪽 시스템이 따로 필요해서 이번 해커톤
  범위에서는 만들 수 없음

필요하면 5분 시연에서 "이런 것도 기술적으로 가능하다"를 라이브로 보여주는 용도로는 쓸 수
있지만, 제출용 코드에 통합하는 기능은 아니다.

## 참고 문서

- `API_CONTRACT.md` — Frontend/Backend REST API 계약
- `DEVELOPER_A_BACKEND_TASKS.md` — 개발자 A 작업 가이드
- `BACKEND_IMPLEMENTATION_PLAN.md` — 백엔드 구현 단계별 계획
